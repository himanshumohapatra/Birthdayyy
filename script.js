// Example of showing an alert after the form submission
const form = document.querySelector('form');
form.addEventListener('submit', function() {
  alert('Form submitted successfully! Check your email for the results.');
});
